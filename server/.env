DJANGO_DEBUG=true
DJANGO_SECRET_KEY=dev-secret-key-change-in-production

# Shop config
SHOP_NAME=My Shop
SHOP_TEMPLATE=default
ALLOW_BACKORDERS=false

# API key is now managed via Django admin → Shop Configuration
# No longer stored in .env
